import React from 'react';
import Loading from '../Shared/Loading/Loading';

const Home = () => {
    return (
        <div>
            <Loading></Loading>

        </div>
    );
};

export default Home;